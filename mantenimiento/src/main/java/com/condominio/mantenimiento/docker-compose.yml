version: '3.8'
services:
  postgres-mantenimiento:
    image: postgres:15-alpine
    container_name: db_mantenimiento
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: mantenimiento_db
    ports:
      - "5432:5432"
    restart: always